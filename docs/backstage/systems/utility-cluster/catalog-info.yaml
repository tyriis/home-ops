---
# yaml-language-server: $schema=https://json.schemastore.org/catalog-info.json
apiVersion: backstage.io/v1alpha1
kind: System
metadata:
  name: utility-cluster
  description: utility kubernetes cluster (Harbor, OpenBao, Pocket-ID)
  tags:
    - k8s
    - talos
    - flux
spec:
  owner: home-ops
  domain: home
