---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: volsync
spec:
  interval: 5m
  chart:
    spec:
      chart: volsync
      version: 0.5.0
      sourceRef:
        kind: HelmRepository
        name: backube-charts
        namespace: flux-system
  values:
    metrics:
      disableAuth: true
