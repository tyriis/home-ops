apiVersion: longhorn.io/v1beta1
kind: BackupTarget
metadata:
  name: nas
  namespace: longhorn-system
spec:
  backupTargetURL: nfs://nas.${SECRET_DOMAIN}:/volume1/backup.k8s.home
  credentialSecret: ''
  pollInterval: 5m0s
