apiVersion: stackgres.io/v1
kind: SGCluster
metadata:
  name: home-assistant
  namespace: home
spec:
  instances: 1
  postgres:
    version: '14.2'
  pods:
    persistentVolume:
      size: 10Gi
      storageClass: longhorn
  # sgInstanceProfile: 'size-xs'
  prometheusAutobind: true
