---
apiVersion: batch/v1
kind: Job
metadata:
  name: label-k3s-worker02
  namespace: system-upgrade
spec:
  backoffLimit: 0
  template:
    spec:
      serviceAccountName: label-nodes
      containers:
        - name: kubectl
          image: ghcr.io/k8s-at-home/kubectl:v1.23.3
          command:
            - "/bin/bash"
            - "-ec"
            - |
              kubectl label node k3s-worker02 plan.upgrade.cattle.io/k3s=true --overwrite=true
      restartPolicy: OnFailure
