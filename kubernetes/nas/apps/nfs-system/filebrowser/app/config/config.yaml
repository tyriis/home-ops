---
server:
  port: 8080
  sources:
    - path: /mnt/media
      config:
        defaultEnabled: true
    - path: /mnt/public
      config:
        defaultEnabled: true
auth:
  adminUsername: admin
  # adminPassword: admin
