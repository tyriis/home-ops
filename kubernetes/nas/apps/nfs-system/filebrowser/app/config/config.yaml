---
server:
  port: 8080
  sources:
    - path: /files/media
      config:
        defaultEnabled: true
    - path: /files/test
      config:
        defaultEnabled: true
    - path: /files/public
      config:
        defaultEnabled: true
auth:
  adminUsername: admin
  adminPassword: admin
