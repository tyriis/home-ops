---
server:
  port: 8080
  database: /data/database.db
  sources:
    - path: /mnt/media
      config:
        defaultEnabled: true
    - path: /mnt/public
      config:
        defaultEnabled: true
auth:
  adminUsername: admin
  # adminPassword: admin
