---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: media
spec:
  storageClassName: zfs-pool
  volumeName: media
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 8000Gi
