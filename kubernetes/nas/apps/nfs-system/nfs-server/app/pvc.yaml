---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: bob
spec:
  accessModes:
    - ReadWriteMany
  capacity:
    storage: 1Ti
  claimRef:
    apiVersion: v1
    kind: PersistentVolumeClaim
    name: bob
    namespace: nfs-system
  csi:
    driver: zfs.csi.openebs.io
    fsType: zfs
    volumeAttributes:
      openebs.io/poolname: pool0/data
    volumeHandle: bob
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - nas02
  persistentVolumeReclaimPolicy: Retain
  storageClassName: zfs-pool0-data
  volumeMode: Filesystem
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: bob
spec:
  storageClassName: zfs-pool0-data
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
