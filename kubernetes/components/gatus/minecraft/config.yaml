---
endpoints:
  - name: ${APP}
    group: minecraft
    url: tcp://${APP}.{NAMESPACE}.svc.cluster.local:25565
    interval: 1m
    ui:
      hide-hostname: true
      hide-url: true
    conditions:
      - "[CONNECTED] == true"
