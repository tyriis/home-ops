---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: &name ${APP}-pocketid
spec:
  oidc:
    # override to use echo instead of echo-server as subdomain
    redirectURL: https://echo.techtales.io/oauth2/callback
