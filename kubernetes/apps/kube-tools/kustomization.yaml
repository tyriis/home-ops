---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./namespace.yaml
  # - ./goldilocks/flux-sync.yaml
  - ./node-feature-discovery/flux-sync.yaml
  - ./reloader/flux-sync.yaml
  # - ./vpa/flux-sync.yaml
