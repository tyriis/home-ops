---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: oci-registry
components:
  - ../../../components/common
  - ../../../components/sops/kube-nas
  - ../../../components/kube-nas
resources:
  - ./harbor/flux-sync.yaml
