---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - secrets.sops.yaml
  - helm-release.yaml
configMapGenerator:
  - name: home-ops-receiver
    files:
      - receiver.yaml=./receiver.yaml
generatorOptions:
  disableNameSuffixHash: true
configurations:
  - kustomize-config.yaml
